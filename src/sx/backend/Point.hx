package sx.backend;


/**
 * Should be replaced with native backend Point class.
 * Point is expected to have decimal `x` and `y` properties measured in pixels.
 */
typedef Point = sx.backend.dummy.Point;